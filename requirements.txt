# Merkraum MCP Server + REST API — Python dependencies
# v1.2 — SUP-149

fastmcp>=2.0.0
neo4j>=5.0.0
qdrant-client>=1.7.0
fastembed>=0.3.0
flask>=3.0.0

# Cognito JWT validation
PyJWT>=2.8.0
requests>=2.31.0

# AWS Bedrock (EU-compliant LLM extraction via Converse API)
boto3>=1.34.0

# PII Gateway (SUP-182) — installed Z1941
presidio-analyzer>=2.2.0
spacy>=3.7.0
# spaCy models (installed separately):
# python -m spacy download en_core_web_lg
# python -m spacy download de_core_news_lg
